version: 1
send_anonymous_usage_stats: true
project_id: "tap-testsource"
default_environment: test
environments:
- name: test
plugins:
  extractors:
  - name: "tap-testsource"
    namespace: "tap_testsource"
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    - about
    - stream-maps
